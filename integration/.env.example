# Nomba sandbox credentials for local testing (Dashboard -> Settings -> API Keys).
# Copy to `.env` and fill in. Never commit `.env`.
CLIENT_ID=your_nomba_client_id
CLIENT_SECRET=your_nomba_client_secret
ACCOUNT_ID=your_nomba_parent_account_id
ENVIRONMENT=sandbox
